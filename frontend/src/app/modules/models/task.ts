export class Task {
  project: string;
  name: string;
  description: string;
  dueDate: string;
  priority: string;
  status: string; //get, but didn't post
  estimation: string;
  assignee: string;
  createdDate: string;
  updateDate: string;
  reporter: string;
  ticketCode: string;
}
